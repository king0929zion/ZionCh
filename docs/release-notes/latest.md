## ZionChat v0.31.68

- 修复 `ZiCodeSettingsScreen` 的 `ColumnScope` 类型错误，解决 `:app:compileReleaseKotlin` 编译失败问题。
- 保留并发布 `v0.31.67` 的完整功能更新：
  - 设置页新增 `Zion Labs` 分组，`AutoSoul` 入口迁移完成。
  - 新增 `ZiCode 设置`页（PAT 配置、真实项目同步、可用工具独立入口）。
  - ZiCode 首页项目列表切换为真实 GitHub 项目源，项目图标更新为新文件夹图标。
  - ZiCode 项目卡片灰阶统一为设置页风格，顶部改为半透明过渡，左上角菜单图标改为主侧边栏同款。
  - ZiCode 对话内容区背景统一，不再分层分色。
