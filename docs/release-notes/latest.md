## ZionChat v0.31.67

- 设置页新增 `Zion Labs` 分组，并将 `AutoSoul` 入口迁移到该分组。
- 新增 `ZiCode 设置`页：
  - 支持配置并保存 GitHub PAT
  - 支持同步用户真实 GitHub 项目列表并选择当前项目
  - 新增“可用工具”独立入口（不在设置页直接展开工具清单）
- ZiCode 首页项目列表改为真实项目源（优先读取 PAT 拉取结果），项目图标更新为新文件夹图标。
- ZiCode 项目卡片配色统一为设置页同款灰色（`#F1F1F1`）。
- ZiCode 顶部区域统一为设置页同款半透明渐变过渡效果；左上角菜单图标改为主侧边栏同款样式。
- ZiCode 对话区背景去掉分层分色，统一为页面底色。
