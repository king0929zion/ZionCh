## ZionChat v0.42.0

- 把设置页的主题外观和配色选择器重新收回单层列表结构：不再是每一项一个独立小框，而是更干净的整体面板 + 分隔线，圆角继续保留，但视觉层级明显更轻。
- 用户消息长按后的工具卡改成贴着被长按消息出现的悬浮菜单，不再居中弹出；菜单也去掉了多层小卡，改成更直接的单层操作列表。
- 长按工具卡继续保留复制、编辑、删除，编辑仍然会把原消息回填到底部输入框。
- About 页面请求最新 Release 和打开 GitHub 仓库时，已经统一切到当前正确项目 `king0929zion/ZionCh`。
